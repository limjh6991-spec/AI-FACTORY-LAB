# Vertical AI Factory 프로젝트 컨텍스트 (AI Assistant 참조용)

> **이 파일을 먼저 읽고 작업을 시작하세요.**
> 마지막 업데이트: 2024-12-21

---

## 📌 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | Vertical AI Factory (다중 에이전트 SQL 생성기) |
| **위치** | `/home/roarm_m3/ai-factory-lab/Project/vertical-ai-factory` |
| **기술 스택** | Python, LangGraph, LangChain, Google Gemini (gemini-2.5-flash) |
| **실행 명령** | `./venv/bin/python src/main.py` |
| **관련 프로젝트** | SpacePro (`/Project/spacepro`) - AI Demo UI 연동 |

---

## 🏗️ Clean Architecture 구조

```
src/
├── config.py                    # LLM 설정 (gemini-2.5-flash)
├── main.py                      # CLI 엔트리포인트 (Rich UI)
├── api_server.py                # FastAPI 서버
│
├── domain/                      # Domain Layer (핵심 비즈니스 로직)
│   ├── state.py                 # AgentState (TypedDict)
│   └── agents/                  # 에이전트 정의
│       ├── analyst.py           # 의도 분석 → Plan 생성
│       ├── writer.py            # Plan → SQL 생성
│       └── critic.py            # SQL 검증 및 피드백
│
├── application/                 # Application Layer (Use Cases)
│   └── graph.py                 # LangGraph 워크플로우
│                                # (Analyst → Writer → Critic, 3회 재시도)
│
├── infrastructure/              # Infrastructure Layer (외부 시스템 연동)
│   └── database/
│       └── mock_db.py           # SQLite Mock DB (costs 테이블)
│
└── interfaces/                  # Interface Layer (API 어댑터)
    └── ...                      # FastAPI 라우터 등
```

### Clean Architecture 계층

```
┌─────────────────────────────────────────────┐
│      Interfaces (interfaces/, api_server)    │  FastAPI, CLI
├─────────────────────────────────────────────┤
│         Application (application/)           │  LangGraph Workflow
├─────────────────────────────────────────────┤
│             Domain (domain/)                 │  Agents, State
├─────────────────────────────────────────────┤
│       Infrastructure (infrastructure/)       │  DB, External APIs
└─────────────────────────────────────────────┘
```

---

## 🎯 현재 진행 상황

### ✅ 완료된 Phase
- **Phase 1**: 기반 환경 구축 (venv, requirements.txt)
- **Phase 2**: Mock DB 및 도구 구현 (SchemaInspector, QueryValidator)
- **Phase 3**: 에이전트 개별 구현 (Analyst, Writer, Critic)
- **Phase 4**: LangGraph 오케스트레이션 (state.py, graph.py, main.py)

### ⏳ 진행 중 / 예정
- **Phase 5**: 시나리오 테스트 (API 할당량 리셋 후)
- **Phase 6**: SpacePro AI Demo 통합

---

## 🔧 개발 시 참고사항

### 1. 환경 변수 (.env)
```bash
GOOGLE_API_KEY=your-google-api-key
OPENAI_API_KEY=sk-proj-xxx...  # GPT 사용 시
```

### 2. LLM 모델 설정 (src/config.py)
```python
# 현재: Gemini 사용
MODEL_CONFIG = {
    "analyst": "gemini-2.5-flash",
    "writer": "gemini-2.5-flash", 
    "critic": "gemini-2.5-flash",
}
```

### 3. 실행 방법
```bash
cd /home/roarm_m3/ai-factory-lab/Project/vertical-ai-factory

# CLI 실행
./venv/bin/python src/main.py

# API 서버 실행
source venv/bin/activate && cd src && python api_server.py
```

### 4. API 할당량 주의
- `gemini-2.5-pro`: Free Tier 사용 불가 (할당량 0)
- `gemini-2.0-flash`: 분당/일일 제한 있음
- `gemini-2.5-flash`: 권장 (Free Tier 사용 가능)

---

## 📁 주요 파일

| 파일 | 설명 |
|------|------|
| `PROJECT_ROADMAP.md` | 개발 로드맵 (Phase 1~6) |
| `단계별_프롬프트.md` | Copilot 사용 가이드 |
| `requirements.txt` | Python 의존성 |
| `.env.example` | 환경 변수 템플릿 |
| `src/config.py` | LLM 모델 팩토리 |
| `src/main.py` | CLI 엔트리포인트 |
| `src/api_server.py` | FastAPI 서버 |
| `src/application/graph.py` | LangGraph 워크플로우 |

---

## 🔗 SpacePro 연동 (Phase 6)

SpacePro `/ai-demo` 페이지와 연동하여 웹 UI 제공:

```
SpacePro (Next.js)          Vertical AI Factory (Python)
┌─────────────────┐         ┌─────────────────────┐
│ /ai-demo        │ ──API──→│ FastAPI Server      │
│ - 질문 입력     │         │ ├─ Analyst Agent    │
│ - 진행 상태     │←─JSON───│ ├─ SQL Writer       │
│ - 결과 표시     │         │ └─ Critic Agent     │
└─────────────────┘         └─────────────────────┘
```

---

**작성자**: AI Assistant  
**마지막 세션**: 2024-12-21
