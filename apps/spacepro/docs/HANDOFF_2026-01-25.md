# SpacePro 생산계획 시스템 - 작업 핸드오프 프롬프트

## 📅 작업일: 2026-01-25

---

## 🎯 오늘 완료된 작업

### 1. 시뮬레이션 데이터 보강 (오전)
```
DB 테이블 변경:
- sp_macode_info: due_date, delivery_qty 컬럼 추가
- sp_eqp_mst: daily_capacity, setup_time, availability_rate 추가
- sp_employee: prcode, prname_detail, efficiency_rate 추가
- sp_work_calendar: 신규 테이블 (2026년 1,095건)
```

### 2. 마스터 화면 개발 (오전)
- `/master/er-diagram` - 13개 sp_ 테이블 ER 다이어그램
- `/master/data-format` - 14개 테이블 샘플 데이터 조회 (5탭)

### 3. 스케줄러 고도화 (오후) ⭐ NEW
```
파일 위치: src/app/plan/scheduler/
- types.ts: Dependency, ResourceAllocation, TimelineConfig 인터페이스
- schedulerEngine.ts: Forward Scheduling + 의존성 생성 + 부하 계산
- SchedulerView.tsx: Bryntum 스타일 UI (파스텔 색상, Manhattan 화살표)
- page.tsx: 실제 DB 연동 + 계약 선택 드롭다운

백엔드:
- scheduling-service/routers/scheduler.py: 신규 API 엔드포인트
  - GET /scheduler/data?contno=계약번호
  - GET /scheduler/contracts
```

### 4. 스케줄러 DB 데이터 연동 (오후) ⭐ NEW
```
연동된 테이블:
- sp_eqp_mst → 설비 목록 (리소스)
- sp_macode_info → 계약/제품 목록 (납기, 수량, 우선순위)
- sp_prcode_detail_info → 라우팅/공정 정보
- sp_work_calendar → 휴무일

실제 데이터:
- 계약 23D220097 선택 시 218개 작업, 19개 제품 표시
- 설비: 벤치1A/C 1호기, F/W(룸1) 6호기 등 실제 DB 설비명
```

### 5. Bryntum 스타일 UI 적용 (오후) ⭐ NEW
```
적용된 디자인:
- 밝은 배경 (bg-gray-50) - 다른 화면과 통일
- 블루 헤더 (#007AD9) - Bryntum 브랜드 컬러
- 파스텔 태스크 바 - 연한 파랑/베이지/초록 등
- 베이지 일자 헤더 - 날짜 식별 용이
- Manhattan 직각 화살표 - 의존성 직관적 표시
- 부하 게이지 - 리소스별 가동률 색상 표시
```

---

## 📂 주요 파일 경로

```
/home/roarm_m3/ai-factory-lab/apps/spacepro/
├── src/app/
│   ├── master/
│   │   ├── er-diagram/page.tsx      # ER 다이어그램
│   │   ├── data-format/page.tsx     # 데이터 포맷
│   │   ├── worker/page.tsx          # 작업자 마스터
│   │   └── pr-detail/page.tsx       # 세부공정정보
│   └── plan/
│       ├── scheduler/               # ★ 고도화 스케줄러 (DB 연동)
│       │   ├── types.ts
│       │   ├── schedulerEngine.ts
│       │   ├── SchedulerView.tsx    # Bryntum 스타일
│       │   └── page.tsx             # API 연동
│       └── simulation/page.tsx      # 기존 시뮬레이션
├── scheduling-service/              # FastAPI 백엔드
│   ├── main.py
│   └── routers/
│       ├── scheduler.py             # ★ 신규 스케줄러 API
│       ├── data_format.py
│       └── simulation.py
└── docs/
    ├── HANDOFF_2026-01-25.md        # 이 파일
    ├── dev-log-2026-01-25.md
    ├── ER_DIAGRAM_SP_TABLES.md
    └── PRODUCTION_SCREENS_STATUS.md
```

---

## ✅ 완료된 작업 (Phase 2-3 일부)

- [x] 실제 DB 데이터 연동 (/scheduler/data API)
- [x] Bryntum 스타일 UI 적용
- [x] 밝은 배경 테마 적용
- [x] 계약 선택 드롭다운

### 남은 작업
- [ ] 휴무일/비가동 시간 표시 (sp_work_calendar 시각화)
- [ ] 납기일 마커 표시
- [ ] 드래그 앤 드롭 (작업 시간/리소스 변경)
- [ ] 이벤트 상세 팝업 모달
- [ ] 스케줄 저장 기능

---

## 💻 실행 환경

```bash
cd /home/roarm_m3/ai-factory-lab/apps/spacepro

# 도커 실행
docker compose up -d

# 접속 URL
- Frontend: http://localhost:3002
- Backend API: http://localhost:8001
- DB: PostgreSQL (localhost:5433)
```

---

## 🔗 참고 URL

| 화면 | URL |
|------|-----|
| 스케줄러 (DB 연동) | http://localhost:3002/plan/scheduler |
| 시뮬레이션 | http://localhost:3002/plan/simulation |
| ER 다이어그램 | http://localhost:3002/master/er-diagram |
| 데이터 포맷 | http://localhost:3002/master/data-format |
| API 문서 | http://localhost:8001/docs |

---

## 📝 GitHub

최신 커밋: (오늘 푸시 예정)
- 스케줄러 DB 연동 + Bryntum 스타일 UI
- https://github.com/limjh6991-spec/AI-FACTORY-LAB
