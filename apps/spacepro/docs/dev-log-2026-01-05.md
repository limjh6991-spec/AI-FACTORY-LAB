# 개발 로그 - 2026-01-05

## 주요 작업

### 1. 이월 로직 실적 기반 개선 ✅
**목적**: 생산 실적(produced_qty) 기반으로 이월 수량 계산

**변경 사항**:
- `tb_order_progress` 테이블에 `source_progress_id` 컬럼 추가 (이월 원본 추적)
- `types.ts`: `ProductOrder.source_progress_id?` 필드 추가
- `simulation.py`: confirm_scenario API에서 source_progress_id 저장
- `page.tsx`: fetchCarryOver, saveScenario에서 source_progress_id 전달

**이월 API 결과** (`GET /carry-over/2026-01`):
| 품목 | 계획 | 실적 | 잔여(이월) |
|------|------|------|------------|
| PROD-003 | 500 | 200 | 300 |
| PROD-001 | 1000 | 700 | 300 |

---

### 2. 시뮬레이션 화면 모듈화 ✅
**목적**: 772줄 단일 파일 → 6개 파일로 분리

**파일 구조**:
```
simulation/
├── page.tsx            (319줄) ← 772줄에서 감소
├── useSimulation.ts    (334줄) - 상태/API 로직
├── types.ts            (84줄)  - 타입 정의
└── components/
    ├── index.ts
    ├── GanttChart.tsx      (128줄)
    ├── MachineUtilization.tsx (43줄)
    ├── OrderList.tsx       (84줄)
    └── ScenarioModal.tsx   (85줄)
```

**개선 효과**:
- page.tsx: 772줄 → 319줄 (59% 감소)
- 각 파일 최대 334줄 (유지보수 용이)
- Custom Hook으로 로직 분리

---

### 3. 간트 차트 탭 추가 ✅
**기능**: 공정별/제품별 뷰 전환

**구현**:
- `GanttChart.tsx`에 탭 UI 추가
- `page.tsx`에 `groupedByProduct` useMemo 추가

**탭 동작**:
- **공정별**: Y축에 공정명, 막대에 제품코드
- **제품별**: Y축에 제품명, 막대에 공정명

---

## 변경 파일 목록

### 신규 생성
- `src/app/plan/simulation/useSimulation.ts`
- `src/app/plan/simulation/components/GanttChart.tsx`
- `src/app/plan/simulation/components/MachineUtilization.tsx`
- `src/app/plan/simulation/components/OrderList.tsx`
- `src/app/plan/simulation/components/ScenarioModal.tsx`
- `src/app/plan/simulation/components/index.ts`

### 수정
- `src/app/plan/simulation/page.tsx` (모듈화)
- `src/app/plan/simulation/types.ts` (source_progress_id 추가)
- `scheduling-service/routers/simulation.py` (source_progress_id 저장)

### 문서
- `docs/screen-documentation.md` (PPT용 화면 설명서)
