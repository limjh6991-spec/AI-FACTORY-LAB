spring:
  application:
    name: ai-factory-backend
  
  # 데이터베이스 설정
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://172.16.200.204:1433;databaseName=도우제조MES시스템TEST;encrypt=false
    username: TEST_MES_USER
    password: Dowoo1!
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

# MyBatis 설정
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.dowinsys.**.dto
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 30

# 서버 설정
server:
  port: 8080
  # servlet:
  #   context-path: /api  # 주석 처리: 컨트롤러에서 이미 /api 사용 중

# 로깅 설정
logging:
  level:
    root: INFO
    com.dowinsys: DEBUG
    org.springframework.web: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
